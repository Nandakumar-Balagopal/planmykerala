import LuxuryChrome from '../../components/LuxuryChrome';
export default function AboutLayout({ children }: Readonly<{ children: React.ReactNode }>) { return <LuxuryChrome>{children}</LuxuryChrome>; }
