// This file defines the layout for the itinerary builder route, including any shared components like headers or footers.
import React from 'react';
import Header from '../../components/Header';
import Footer from '../../components/Footer';

const ItineraryLayout = ({ children }) => {
    return (
        <div>
            <Header />
            <main>{children}</main>
            <Footer />
        </div>
    );
};

export default ItineraryLayout;