import type { Metadata } from 'next';
import Link from 'next/link';
import { ArrowLeft, ArrowRight, Check } from 'lucide-react';
import { journal } from '../../../lib/journal';

export function generateStaticParams() { return Object.keys(journal).map((slug) => ({ slug })); }
export function generateMetadata({ params }: { params: { slug: string } }): Metadata { const story = journal[params.slug]; return story ? { title: story.title, description: story.description, alternates: { canonical: `/blog/${params.slug}` }, openGraph: { type:'article', title:story.title, description:story.description, images:[story.image] }, twitter:{ card:'summary_large_image' } } : {}; }
export default async function Story({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const story = journal[slug]; if (!story) return <main />; const schema = { '@context':'https://schema.org', '@type':'Article', headline:story.title, description:story.description, image:story.image, datePublished:'2026-07-28', dateModified:'2026-07-28', author:{ '@type':'Organization', name:'PlanMyKerala' }, publisher:{ '@type':'Organization', name:'PlanMyKerala' }, mainEntityOfPage:`https://planmykerala.com/blog/${slug}` }; return <main className="article-page"><section className="article-hero" style={{ backgroundImage:`linear-gradient(90deg,rgba(10,35,29,.83),rgba(10,35,29,.25)),url(${story.image})` }}><div><Link href="/blog" className="article-back"><ArrowLeft size={15}/> Journal</Link><p className="eyebrow light"><i /> {story.category.toUpperCase()} · {story.readTime.toUpperCase()}</p><h1>{story.title}</h1><p>{story.description}</p></div></section><article className="article-body"><p className="article-lead">{story.opening}</p><div className="article-rule" /><h2>See it differently.</h2><p>{story.perspective}</p><aside><p className="eyebrow"><i /> MAKE SPACE FOR</p>{story.moments.map((item) => <div key={item}><Check size={16}/><span>{item}</span></div>)}</aside><h2>A journey with room in it.</h2><p>These are not instructions to follow perfectly. They are an invitation to travel with a little more attention — to choose fewer things, stay a little longer, and let Kerala meet you where you are.</p><div className="article-cta"><p className="eyebrow light"><i /> MAKE IT YOURS</p><h2>Want to follow this feeling?</h2><p>Our Kerala team can shape this into a private journey around your time, interests and pace.</p><Link href="/contact" className="button gold">Start a conversation <ArrowRight size={16}/></Link></div></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}} /></main>; }
