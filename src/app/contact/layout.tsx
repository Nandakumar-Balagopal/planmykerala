import LuxuryChrome from '../../components/LuxuryChrome';
export default function ContactLayout({ children }: Readonly<{ children: React.ReactNode }>) { return <LuxuryChrome>{children}</LuxuryChrome>; }
