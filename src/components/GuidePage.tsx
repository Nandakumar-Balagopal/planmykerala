import Link from 'next/link';
import { ArrowRight, CheckCircle2 } from 'lucide-react';
import type { Guide } from '../lib/guide-content';

export default function GuidePage({ guide, kind, slug }: { guide: Guide; kind: string; slug: string }) {
 const schema = { '@context':'https://schema.org', '@type': kind === 'Itinerary' ? 'Trip' : 'TouristAttraction', name:guide.title, description:guide.description, image:guide.image, url:`https://www.planmykerala.com/${kind.toLowerCase()}s/${slug}` };
 return <main className="guide-page"><section className="guide-hero" style={{backgroundImage:`linear-gradient(90deg,rgba(10,35,29,.84),rgba(10,35,29,.26)),url(${guide.image})`}}><div><p className="eyebrow light"><i/> {guide.eyebrow}</p><h1>{guide.title}</h1><p>{guide.description}</p><Link href="/#planner" className="button gold">Plan this journey <ArrowRight size={16}/></Link></div></section><article className="guide-body"><p className="guide-lead">{guide.summary}</p><div className="guide-columns"><section><p className="eyebrow"><i/> WHAT MAKES IT WORK</p>{guide.highlights.map((item)=><p className="guide-highlight" key={item}><CheckCircle2 size={17}/>{item}</p>)}</section><aside><p className="eyebrow"><i/> BEST TIME</p><p>{guide.bestTime}</p><Link href="/contact" className="text-link">Ask a local expert <ArrowRight size={15}/></Link></aside></div><section className="guide-faq"><p className="eyebrow"><i/> COMMON QUESTIONS</p><h2>Good to know<br/><em>before you go.</em></h2>{guide.faqs.map((faq)=><details key={faq.q}><summary>{faq.q}</summary><p>{faq.a}</p></details>)}</section><section className="guide-cta"><p className="eyebrow light"><i/> MAKE IT PERSONAL</p><h2>Let’s design it<br/><em>around you.</em></h2><p>Tell us your dates, travel style and who is coming. A Kerala journey designer will turn this guide into a route that feels effortless.</p><Link href="/contact" className="button gold">Request your tailored route <ArrowRight size={16}/></Link></section></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></main>;
}
